# Changelog

## Unreleased
- Redact signed URL query secrets such as `sig`, `signature`, `X-Amz-Signature`, and `X-Goog-Signature` when environment values are explicitly included.
- Add regression coverage for generic, Azure-style, AWS-style, and Google-style signed URLs.

## 1.0.0
- Initial public OSS release.
